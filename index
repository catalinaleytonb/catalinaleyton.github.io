<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Catalina Leyton - Portafolio</title>
  <link rel="stylesheet" href="style.css" />
  <style>
    /* Todo tu CSS que enviaste, corregido y adaptado */

    /* Fuente desde Google Fonts */
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap');

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      color: #f0f0f5;
      min-height: 100vh;
      padding: 40px;
      background: #0f172a; /* fondo oscuro base para la animación */
      overflow-x: hidden;
      position: relative;
      z-index: 0;
    }

    /* Animación de fondo está en el canvas */

    /* Contenedor principal */
    .container {
      max-width: 900px;
      margin: auto;
      background: rgba(31, 41, 55, 0.95);
      padding: 50px 60px;
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.5);
      border: 1px solid #3b82f6;
      position: relative;
      z-index: 10;
    }

    /* Títulos */
    h1, h2 {
      color: #3b82f6;
      margin-bottom: 20px;
      text-shadow: 0 1px 5px rgba(59, 130, 246, 0.5);
    }

    h1 {
      font-weight: 700;
      font-size: 3rem;
      letter-spacing: 2px;
      text-align: center;
      margin-bottom: 50px;
    }

    h2 {
      font-weight: 600;
      font-size: 2rem;
      margin-top: 50px;
      border-bottom: 3px solid #3b82f6;
      padding-bottom: 8px;
    }

    /* Texto general */
    p, li {
      font-weight: 300;
      line-height: 1.7;
      font-size: 1.05rem;
      color: #d1d5db;
    }

    ul {
      padding-left: 25px;
      margin-top: 15px;
    }

    li {
      margin-bottom: 15px;
    }

    /* Código inline */
    code {
      background-color: #272f4a;
      padding: 5px 10px;
      border-radius: 6px;
      font-family: 'Source Code Pro', monospace;
      font-size: 1rem;
      color: #60a5fa;
      box-shadow: inset 0 0 5px #2563eb;
    }

    /* Imagen con efecto */
    img {
      height: 450px;
      display: block;
      margin: 0 auto 70px auto;
      border-radius: 15px;
      box-shadow: 0 15px 40px rgba(59, 130, 246, 0.5);
      transition: transform 0.4s ease;
    }

    img:hover {
      transform: scale(1.05);
    }

    /* Botones */
    .btn {
      display: inline-block;
      background: #2563eb;
      color: #fff;
      text-decoration: none;
      padding: 14px 28px;
      margin-top: 15px;
      margin-right: 15px;
      border-radius: 30px;
      font-weight: 600;
      font-size: 1rem;
      box-shadow: 0 8px 20px rgba(37, 99, 235, 0.4);
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }

    .btn:hover {
      background-color: #1d4ed8;
      box-shadow: 0 10px 30px rgba(29, 78, 216, 0.7);
    }

    /* Contacto */
    .contact-buttons {
      margin-top: 25px;
    }

    /* Responsive */
    @media (max-width: 600px) {
      body {
        padding: 20px;
      }

      .container {
        padding: 30px 25px;
      }

      h1 {
        font-size: 2.2rem;
      }

      h2 {
        font-size: 1.5rem;
      }

      img {
        height: 300px;
        margin-bottom: 40px;
      }

      .btn {
        padding: 12px 20px;
        font-size: 0.9rem;
        margin-right: 10px;
      }
    }

    /* Texto dinámico en Experiencia Laboral */
    h2.dinamico {
      background: linear-gradient(270deg, #3b82f6, #60a5fa, #3b82f6);
      background-size: 400% 400%;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      color: transparent;
      animation: moverBrillo 6s ease infinite;
    }

    @keyframes moverBrillo {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Canvas de fondo animado */
    #background-canvas {
      position: fixed;
      top: 0;
      left: 0;
      z-index: -1;
      width: 100vw;
      height: 100vh;
      background: #0f172a;
      display: block;
    }
  </style>
</head>
<body>
  <!-- Canvas para animación de fondo -->
  <canvas id="background-canvas"></canvas>

  <div class="container">
    <h1>Portafolio</h1>
    <img src="logo.png" alt="Logo Catalina Leyton" />

    <p><strong>Técnico Medio en Administración de Empresas</strong> - Instituto Superior de Comercio 2</p>
    <p><strong>Ingeniería en Informática</strong> - Universidad Santo Tomás (Séptimo semestre, 2021–2025)</p>

    <div class="section">
      <h2 class="dinamico">Experiencia Laboral</h2>
      <ul>
        <li><strong>BGR S.A (2025 - Abril)</strong> – Reponedora Externa<br />Coordinación y ejecución de tareas en terreno.</li>
        <li><strong>Jumbo (2015 - Actualidad)</strong> – Reponedora<br />Liderazgo en tareas logísticas, inventario y atención al cliente.</li>
        <li><strong>Entel Contact Center (Ago 2023 - Mar 2024)</strong> – Soporte Mesa de Ayuda<br />
          Soporte remoto en software, hardware y redes. Uso de TeamViewer, Jira, Active Directory y SAP.
        </li>
      </ul>
    </div>

    <div class="section">
      <h2>Habilidades Técnicas</h2>
      <ul>
        <li>Ciberseguridad: detección de amenazas, TCP/IP, Active Directory.</li>
        <li>Cloud: Azure (VMs, configuración básica), ambientes virtualizados.</li>
        <li>Redes: LAN, WAN, MPLS, Packet Tracer.</li>
        <li>Sistemas operativos: Kali Linux, Debian, Windows, Android.</li>
        <li>Bases de datos: MariaDB, MySQL, SQL.</li>
        <li>Programación: PHP, C++, Java, GitHub.</li>
        <li>Desarrollo: Unity 2D/3D, patrones de diseño.</li>
        <li>Herramientas: Jira, SAP, TeamViewer.</li>
      </ul>
    </div>

    <div class="section">
      <h2>Proyectos</h2>
      <ul>
        <li>
          <strong>Instalación de MariaDB en Debian</strong><br />
          Configuración completa de MariaDB en máquina virtual con Debian. Se usaron comandos como <code>apt install mariadb-server</code>, y se configuró acceso seguro.<br />
          <a class="btn" href="mariadb_vm_dbeaver.pdf" target="_blank">📄 Ver PDF</a>
          <a class="btn" href="catalinaleyton.pdf" target="_blank">📄 Ver PDF</a>
        </li>
      </ul>
    </div>

    <div class="section" id="contacto">
      <h2>Contacto</h2>
      <p>Estoy disponible para proyectos, colaboraciones o consultas. ¡Conéctate conmigo!</p>
      <div class="contact-buttons">
        <a href="mailto:leytoncatalina29@gmail.com" class="btn contact-btn email" target="_blank" rel="noopener">✉️ Email</a>
        <a href="https://www.linkedin.com/in/catalina-leyton-bascuñan-605b54288" class="btn contact-btn linkedin" target="_blank" rel="noopener">🔗 LinkedIn</a>
        <a href="https://github.com/catalinaleytonb" class="btn contact-btn github" target="_blank" rel="noopener">🐱 GitHub</a>
        <a href="https://wa.me/56957436358" class="btn contact-btn whatsapp" target="_blank" rel="noopener">💬 WhatsApp</a>
      </div>
    </div>
  </div>

  <!-- Script para animación del fondo dinámico -->
  <script>
    const canvas = document.getElementById('background-canvas');
    const ctx = canvas.getContext('2d');
    let width, height;
    let points = [];

    function resizeCanvas() {
      width = window.innerWidth;
      height = window.innerHeight;
      canvas.width = width;
      canvas.height = height;
      initPoints();
    }

    function initPoints() {
      points = [];
      for (let i = 0; i < 100; i++) {
        points.push({
          x: Math.random() * width,
          y: Math.random() * height,
          vx: (Math.random() - 0.5) * 0.5,
          vy: (Math.random() - 0.5) * 0.5
        });
      }
    }

    function drawPoints() {
      ctx.clearRect(0, 0, width, height);
      for (let i = 0; i < points.length; i++) {
        const p1 = points[i];
        p1.x += p1.vx;
        p1.y += p1.vy;

        if (p1.x < 0 || p1.x > width) p1.vx *= -1;
        if (p1.y < 0 || p1.y > height) p1.vy *= -1;

        ctx.beginPath();
        ctx.arc(p1.x, p1.y, 1.5, 0, Math.PI * 2);
        ctx.fillStyle = '#60a5fa';
        ctx.fill();

        for (let j = i + 1; j < points.length; j++) {
          const p2 = points[j];
          const dx = p1.x - p2.x;
          const dy = p1.y - p2.y;
          const dist = Math.sqrt(dx * dx + dy * dy);

          if (dist < 100) {
            ctx.beginPath();
            ctx.moveTo(p1.x, p1.y);
            ctx.lineTo(p2.x, p2.y);
            ctx.strokeStyle = 'rgba(96,165,250,' + (1 - dist / 100) + ')';
            ctx.lineWidth = 0.5;
            ctx.stroke();
          }
        }
      }
    }

    function animate() {
      drawPoints();
      requestAnimationFrame(animate);
    }

    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();
    animate();
  </script>
</body>
</html>
